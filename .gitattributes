# Shell scripts must use LF line endings, otherwise `bash script.sh` fails with
# errors like "set: -: invalid option" on machines where core.autocrlf=true.
*.sh text eol=lf
# Keep this file itself LF so its patterns are parsed correctly.
.gitattributes text eol=lf
